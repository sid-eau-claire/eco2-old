'use strict';

/**
 * commissiondistribution controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::commissiondistribution.commissiondistribution');
