'use strict';

/**
 * commissiondistribution router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::commissiondistribution.commissiondistribution');
