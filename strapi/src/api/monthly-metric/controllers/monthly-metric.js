'use strict';

/**
 * monthly-metric controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::monthly-metric.monthly-metric');
